// Get the team name and member emails provided by user.
function getTeamInfo() {
    const url = '/team';    // Connect to teamServlet.
    fetch(url).then((???) => {
        var teamName = document.getElementById("team-name");
        var memberEmails = document.getElementById("member-emails").split(', '); // Array of the emails.

        // Utilize doPost - ask Faisal

    });
}