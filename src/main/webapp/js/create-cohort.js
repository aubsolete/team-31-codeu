// Get the cohort name provided by user.
function getCohortInfo() {
    const url = '/cohort';  // Connect to cohortServlet.
        fetch(url).then((???) => {
            var cohortName = document.getElementById("cohort-name");

            // Utilize doPost - ask Faisal

        });
}